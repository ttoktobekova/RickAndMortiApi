<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="chipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/chip_selector_color</item>
        <item name="chipStrokeColor">@color/chip_selector_color</item>
        <item name="chipCornerRadius">8dp</item>
        <item name="chipStrokeWidth">2dp</item>
        <item name="chipBackgroundColor">@color/black</item>
    </style>

    <style name="RadioBtn" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:layout_marginStart">12dp</item>
    </style>

    <style name="TextColor">
        <item name="android:textColor">@color/blue</item>
    </style>
</resources>